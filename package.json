{
  "scripts": {
    "start": "pm2 start --name  \"websites\" index.js",
    "stop": "pm2 stop index.js",
    "dev": "nodemon index.js --dev",
    "push": "git add . --all && git commit -m \"`date`\" && git push origin master",
    "pull": "git pull origin master",
    "update": "git pull origin master && npm install && pm2 restart webserver"
  },
  "devDependencies": {},
  "dependencies": {
    "body-parser": "^1.18.3",
    "bouncy": "^3.2.2",
    "compression": "^1.7.3",
    "express": "^4.16.4",
    "vhost": "^3.0.2"
  }
}
